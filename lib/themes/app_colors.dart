import 'package:flutter/material.dart';

class AppColors {
  static const Color backgroundColor = Color(0xFF090909);
  static const Color authTopTextColor = Colors.white;
  static  Color authTextFieldStrokeColor = Colors.white.withOpacity(0.2);
  static Color authTextFieldBackgroundColor = Colors.white.withOpacity(0.1);
  static Color authTextFieldLabelColor = Colors.white.withOpacity(0.5);
  static const Color authButtonColor = Color(0xFFE50914);
  static const Color authButtonTextColor = Colors.white;
  static Color differentLoginButtonColor = Colors.white.withOpacity(0.1);

}